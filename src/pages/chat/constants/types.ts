export enum revealStatus {
    WAITING = "WAITING",
    ACCEPTED = "ACCEPTED",
    STANDBY = "REVEAL",
    CONFIRM = "CONFIRM",
}

export enum matchStatus {
    WAITING = "WAITING",
    ACCEPTED = "ACCEPTED",
    STANDBY = "MATCH",
    CONFIRM = "CONFIRM",
}